import React from "react";

const useAuth = () => {
  const loginUser = async () => {
    try {
        
    } catch (err) {
      console.log(err);
    }
  };
  return <div>useAuth</div>;
};

export default useAuth;
